#include<iostream>
using namespace std;
class TreeNode{
    int Tree(value){
        public:
        int data = value, left = NULL, right = NULL, content = NULL;
        // TreeNode*left;
        // TreeNode*right;
        };
    int Insert(value,content = None){
        if(value<value):
            if(left==NULL):
                left = TreeNode(value, content)
                // content = content;
            else:
                Insert(left,content);
        else:
            if(right==NULL):
                right = TreeNode(value, content);
            else:
                Insert(right, content);
        };

};

int main(){
    tree = TreeNode(1);
    tree.insert(5)
    tree.insert(2)
    tree.insert(4)
    tree.insert(1)
    tree.insert(2)
    tree.insert(4)
    
}